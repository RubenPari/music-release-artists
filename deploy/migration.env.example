# GitHub Actions secret: MIGRATION_DATABASE_URL
# Used only by the App Platform PRE_DEPLOY migrate job (role mra_migrator).
# Prefer sslmode=require on App Platform (no CA file mount).

DATABASE_URL=postgres://mra_migrator:replace-me@private-or-public-db-host:25060/music?sslmode=require
