# Copy to /etc/music-release-artists/migration.env (mode 0640).
# Use a dedicated database user that can apply schema migrations.
DATABASE_URL=postgres://mra_migrator:replace-me@private-db-host:25060/music?sslmode=verify-full
